<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Kanit:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="index.css" />
    <title>Home</title>
  </head>
  <body>
    <div id="container">
      <h3>CREATE POST</h3>
      <form action="/" method="post">
        <div>
          <label for="postName">Title</label> <br />
          <input type="text" id="postName" name="title" placeholder="Title" />
        </div>
        <div>
          <label for="postBody">Body</label> <br />
          <textarea
            type="text"
            id="postBody"
            name="body"
            placeholder="Body"
          ></textarea>
        </div>
        <button type="submit">Submit</button>
      </form>

      <div id="postContainer">
        <% for (let i = 0; i < posts.length ; i++) { %>
        <div class="post">
          <div class="individualPostId"><%= posts[i].id %></div>
          <div class="postClass">
            <h4 class="individualPostTitle"><%= posts[i].title %></h4>
            <div class="individualPostBody"><%= posts[i].body %></div>
          </div>
        </div>
        <% } %>
      </div>
    </div>
    <script src="/index.js"></script>
  </body>
</html>
